# Ft_nmap
A custom implementation of a network port scanner inspired by Nmap, built in C using raw sockets, pcap, and multithreading. The project supports multiple scan types (SYN, NULL, ACK, FIN, XMAS, UDP), configurable threading for performance, and flexible input (IP or file), providing fast and structured network analysis results.
